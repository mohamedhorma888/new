Library Management Prototype

Quick prototype demonstrating basic borrow/return flows with in-memory repos.

Setup

Install dev deps:

```bash
npm install
```

Run demo

```bash
npx ts-node src/index.ts
```
