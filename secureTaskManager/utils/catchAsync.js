// Utility to wrap async route handlers and pass errors to error-handling middleware
const catchAsync = (fn) => {
  return (req, res, next) => {
    fn(req, res, next).catch(next);
  };
};

module.exports = catchAsync;
